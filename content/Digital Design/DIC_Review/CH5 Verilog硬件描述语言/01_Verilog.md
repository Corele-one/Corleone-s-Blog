关于Verilog可以先看这个[视频](https://www.bilibili.com/video/BV1iv4y1F7Km/)来学习
**在网页环境里会把非阻塞赋值渲染成小于等于号("<=")，请读者注意**
# Module
Verilog中最重要的概念就是模块，在有了模块这个概念之后我们就能够更好地进行层次化的设计，如下图所示：

![[file-20260614223956286.png|831]]

在 Verilog 中，信号、常量和其他定义的适⽤范围（ scope ) 对每个模块来说都是**局部性质**的；只有通过使⽤被声明的输⼊和输出信号，才能让它们的值在模块之间进⾏传递。
## Verilog模块声明的基本语法
它以关键字 module开头，跟着的是模块名称的标识符和模块输⼈输岀端⼝的标识符列表。输⼊输出端⼝（ input and output ports ) 是该模块与其他模块互相沟通的信号，可以把它们想象成连线，因为它们正是模块通常要实现的东西。

![[file-20260614224550405.png|534x333]]

**module定义完了之后一定要加分号！！！勿漏**
声明模块的时候也可以采用ANSI的风格声明，这种声明的好处就是声明的时候可以同时声明端口名、信号方向和可选的取值范围

![[file-20260614225324391.png|800]]

如果没有提及端口的类型的话，**默认为1位的wire**
## Verilog输入/输出声明的语法

![[file-20260614224645734.png|404x185]]

如果要声明多位或“ 向量” 的信号，则还要包括⼀个范围说明（ range specification ) 指定 [msb:lsb]，表 5-2 中的最后三⾏就是这种说明。位宽可以是升序或降序的，如[7:0]、[0:7] 和 [13:20], 这些都是正确的8位宽。

# 逻辑系统、网格、变量和常量
## 四值逻辑系统

![[file-20260614225523310.png|600]]

## 逐位布尔操作符(注意reduction操作)

![[file-20260614225642928.png|389x210]]

**⚠️单个&表示按位操作，&&表示逻辑操作**

**💡：&(4'b0011)**：表示按位进行缩减与操作，相当于 0&0&1&1

## Verilog中的两类信号
### 网格(Net)
⽹格（net ) ⼤致对应于物理电路中的连线，在 Verilog 结构模型中提供模块与其他元件之间的连通性。在 Verilog 模块的输⼊/输出端⼝列表中的信号，常常就是⽹格。
最常见的网格类型是wire，除wire之外还有supply0，supply1等网络类型可以被认为是连接到电源轨上的永久性连线。

![[file-20260614225946370.png|316]]

Verilog ⽹格声明（net declaration ) 的语法类似于输⼈ / 输出声明。对于向量⽹格(Vector)，则需在标识符列表的前⾯指定它的“界”。
![[file-20260614231019002.png|814]]

### 变量(Variable)

是在 Verilog 程序执⾏期间⽤于存储数值的，它们并没有实际的物理意义，仅用在**过程编码**中。最常用的两种变量类型是reg和integer变量

![[file-20260614231257090.png|807]]

## 网格和变量的区别(wire vs reg)

![[file-20260614231606990.png|803]]

[[wire vs reg]]

![[file-20260614232004350.png|816]]

### 端口声明与例化中的类型约束

上述区别直接决定了 wire 和 reg 在**声明模块**和**实例化模块**时端口可接受的类型。

#### 1. 声明模块时的端口声明

声明模块时，端口方向与类型一起或分开指定：

```verilog
// ANSI 风格：端口名、方向、类型一起声明
module my_module (
    input  wire       a,        // ✅ input 只能为 wire（net类型）
    input  wire [3:0]  c,       // ✅ wire 向量输入
    output wire       y1,       // ✅ output 可以为 wire
    output reg        y2        // ✅ output 也可以为 reg
);
    // y2 作为 reg 输出，必须在过程块中赋值
    always @(*) begin
        y2 = a & c[0];
    end
    // y1 作为 wire 输出，用 assign 连续赋值
    assign y1 = c[0];
endmodule
```

**声明模块时端口可选类型小结：**

| 端口方向 | 可选类型 | 说明 |
|---------|---------|------|
| `input` | **只能是 wire** | 输入信号值来自模块外部，模块内部只读取不赋值，故只能为 net 类型 |
| `output` | **wire 或 reg** | wire 用 `assign` 驱动；reg 在 `always/initial` 过程块中赋值 |

> ⚠️ 如果不指定端口类型，**默认为1位wire**。在最常见的使用场景中，声明模块时 input 和 output 都声明为 wire 即可，只有当 output 需要在 `always/initial` 过程块中赋值时才声明为 reg。

#### 2. 实例化模块时的端口连接

在模块外部进行例化时，需要将外部信号连接到模块端口：

```verilog
module top;
    reg  [7:0] data_in;    // reg 变量
    wire [7:0] data_out;   // wire 网格
    wire       clk;

    my_module u_inst (
        .a  (clk),          // ✅ input 端口：可接 wire
        .b  (data_in),      // ✅ input 端口：也可接 reg（外部驱动模块输入）
        .y1 (data_out[0]),  // ✅ output 端口：接 wire
        .y2 (data_in[0])    
        // ❌ ERROR！output 端口不能接 reg
    );
endmodule
```

**实例化模块时端口连接可接受类型小结：**

| 端口方向 | 可连接的外部类型 | 说明 |
|---------|---------------|------|
| `input` 端口 | **wire 或 reg** | 实例化时是从外部驱动模块输入，reg 和 wire 均可 |
| `output` 端口 | **只能接 wire** | reg 不能通过模块端口连接驱动，因为需要从模块内部改变 reg 变量的值 |

#### 3. 核心规则一句话总结

> **声明模块时**：`input` 只能声明为 `wire`；`output` 可以是 `wire` 或 `reg`。
>
> **实例化模块时**：`input` 端口可以连接 `wire` 或 `reg`；`output` 端口只能连接 `wire`。

这正是图中定义的体现：**Net 类型可以在模块端口连接中接收 reg 类型的驱动**（实例化时 input 可接 reg），而 **reg 类型不能连接到模块实例化的输出端口**（实例化时 output 只能接 wire）。

## 数值文字的书写
如果不加其他修饰的话，那么字串会被解释为十进制数字

**n'Bdd⋯d**

![[file-20260614232633477.png|755]]

### Verilog的参数声明

![[file-20260614232805974.png|810]]

# 向量和操作符

## Vector的声明、位选择

![[file-20260614233009168.png|822]]

## 串接 and 复制

![[file-20260614233147911.png|811]]

{byte1,byte2,byte3,byte4}组合成一个32位的向量
{2{byte1},2{byte2}}也能够组合成一个32位的向量(n{ }复制操作符)

## 逐位布尔操作符对vector的作用

![[file-20260614233419836.png|810]]

## Verilog中的算数操作符

![[file-20260614233828091.png|421]]

默认把向量当作⽆符号整数来处理；但是，也可以当作有符号的**补码**整数

![[file-20260614233936377.png|826]]

# 数组

![[file-20260614234209993.png|803]]

注意和Vector中的位宽界定的位置做区分，看懂所举的实例

# 逻辑操作符和表达式

**逻辑操作注意和逐位布尔操作做区分**，逐位布尔操作中，取非为~,在逻辑操作中，取非为!

![[file-20260614234424137.png|811]]

逻辑相等和不相等操作符是对操作数⼀位⼀位地进⾏⽐较的,仅当对应的位都相等时才是相等的
## 条件操作符

A = B ? C : D;
如果B为真，那么A=C，如果B为假，那么A=D
并且，条件操作符是可以嵌套使用的

## 关于x和z值

![[file-20260614235629789.png|795]]

