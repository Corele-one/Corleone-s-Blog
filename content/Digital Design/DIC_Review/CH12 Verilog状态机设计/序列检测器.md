>[!question]
>设计一个序列检测器（4-bit 1101 detector）
>该序列检测器考虑overlap(重叠)的情况:即1101101会检测到序列出现两次

# Moore型的设计方法

## 1. 先画状态转移图
  
![[JPEG图像-4B54-A00D-41-0.jpeg]]

```verilog
module fsm(CLK,X,Z,RST);
	input CLK,X;//X为输入的序列
	output Z;
	reg [2:0] S,Snext;//5个状态需要3位来编码
	
	parameter [2:0] INIT=3'b000;
	parameter [2:0] S0  =3'b001;
	parameter [2:0] S1  =3'b010;
	parameter [2:0] S2  =3'b011;
	parameter [2:0] S3  =3'b100;
	//状态存储器
	always@(posedge CLK) begin
		if(RST)
			S<=INIT;
		else
			S<=Snext;
	end
	
	//次态逻辑
	always@(S,X) begin
		case(S)
			INIT:Snext=X?S0:INIT;
			S0  :Snext=X?S1:INIT;
			S1  :Snext=~X?S2:INIT;
			S2  :Snext=X?S3:INIT;
			S3  :Snext=~X?INIT:S1;
			default:Snext=INIT;
		endcase
	end
	
	//输出逻辑
	assign Z=(S==S3)?1'b1:1'b0;
endmodule
```

## Mealy型状态机设计

### 1. 先画状态转移图

状态转移图(箭头上为`输入/输出`)
![[file-20260701210016195.png|839]]

### 2. 状态转移表

| 状态   | 含义       | X=0       | X=1         |
| ---- | -------- | --------- | ----------- |
| INIT | 无前缀      | INIT, Z=0 | S₀, Z=0     |
| S₀   | 前缀 `1`   | INIT, Z=0 | S₁, Z=0     |
| S₁   | 前缀 `11`  | S₂, Z=0   | S₁, Z=0     |
| S₂   | 前缀 `110` | S₂, Z=0   | **S₀, Z=1** |

> [!tip] 输出时机
> Mealy型的输出由 **(当前状态, 当前输入)** 共同决定，在 S₂ 收到 X=1 时 **立即输出 Z=1**，不需要额外的 S₃ 状态。

### 3. Verilog代码（考虑overlap）

```verilog
module fsm_mealy(CLK, X, Z, RST);
    input CLK, X;
    output Z;
    reg [1:0] S, Snext; // 4个状态只需2位编码

    parameter [1:0] INIT = 2'b00;
    parameter [1:0] S0   = 2'b01; // 前缀 1
    parameter [1:0] S1   = 2'b10; // 前缀 11
    parameter [1:0] S2   = 2'b11; // 前缀 110

    // 状态存储器
    always @(posedge CLK) begin
        if (RST)
            S <= INIT;
        else
            S <= Snext;
    end

    // 次态逻辑
    always @(S, X) begin
        case (S)
            INIT: Snext = X ? S0 : INIT;
            S0:   Snext = X ? S1 : INIT;
            S1:   Snext = X ? S1 : S2;
            S2:   Snext = X ? S0 : S2; // overlap: X=1→S0
            default: Snext = INIT;
        endcase
    end

    // 输出逻辑（Mealy: 取决于 状态+输入）
    always @(S, X) begin
        Z = (S == S2) & X; // S₂且X=1时输出1
    end
endmodule
```

---

## Moore型 vs Mealy型 对比

|           | **Moore型**               | **Mealy型**               |
| --------- | ------------------------ | ------------------------ |
| **输出取决于** | 仅当前状态                    | 当前状态 **+** 当前输入          |
| **状态数**   | 5个（INIT, S₀, S₁, S₂, S₃） | **4个**（INIT, S₀, S₁, S₂） |
| **编码位宽**  | 3 bit                    | **2 bit**                |
| **输出时机**  | 滞后一个时钟周期                 | **当周期即时输出**              |
| **输出逻辑**  | `Z = (S == S3)`          | `Z = (S == S2) & X`      |
| **优点**    | 输出稳定，无毛刺                 | 响应快，省资源                  |
| **缺点**    | 响应慢，多一个状态                | 输出可能有毛刺                  |

> [!important] 核心区别
> - **Moore**：检测到完整序列后，在**下一个时钟沿**才输出 Z=1（需要一个专门的"检测完成"状态 S₃）
> - **Mealy**：在**当前时钟周期内**，只要条件满足就立即输出 Z=1（不需要 S₃ 状态）
