在过程块中使用的循环语句，用于生成组合逻辑
事例：位反转
```verilog
module reverse_bits(
    input  [3:0] in,
    output reg [3:0] out
);
    integer i;  // 声明循环变量
    always @(*) begin
        for (i = 0; i < 4; i = i + 1) begin
            out[i] = in[3-i];  // 过程赋值
        end
    end
endmodule
```

